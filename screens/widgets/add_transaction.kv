#:import utils kivy.utils
<AddTransaction>:
    # size_hint: (, )
    # size: ('', '')  
    auto_dismiss: True         # clicking outside closes it
    background_color: [0, 0, 0, 0.6]  # for the dim background

    # popup frontend layout
    # BoxLayout:
        # ...