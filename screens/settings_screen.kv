#:import utils kivy.utils
# Write screen frontend here
<SettingsScreen>:
    BoxLayout:
        orientation: 'vertical'
        padding: (10)

        Label:
            text: "Settings"
            color: (0, 0, 0, 1)
            font_size: "40sp"
            size_hint: (1, None)
            height: 100